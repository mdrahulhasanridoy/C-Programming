#include <stdio.h>

int main()
{
    int a = 5;
    int b = 3;
    int c = a + b;
    printf("Sum of %d and %d is %d\n", a, b, c);
    printf("The remainder when a is divided by b is : %d\n", a % b);
}
